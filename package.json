{
  "name": "voice-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Phone-based bilingual (Hindi/English) conversational voice agent, fully self-hosted speech, telephony via your own Asterisk + AudioSocket (any SIP trunk provider + local Whisper STT + Claude + local Piper/MeloTTS TTS)",
  "main": "dist/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.7.5",
    "tsx": "^4.19.1",
    "typescript": "^5.6.3"
  }
}
